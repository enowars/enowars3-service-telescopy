version: '3'

services:
  telescopy-service:
    build: ./src
    volumes: 
      - ./src:/usr/src/app
    ports: 
      - "8000:8000"
    depends_on:
      - redis
  redis:
    image: redis
    ports:
      - "6379:6379"